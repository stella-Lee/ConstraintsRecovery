/*
C:/Users/JBNU/git/CCIntegrator/CCInegrator/./temp/TankWar_P1/src/Tank$$einfrieren.java
C:/Users/JBNU/git/CCIntegrator/CCInegrator/./temp/TankWar_P2/src/Tank$$einfrieren.java
C:/Users/JBNU/git/CCIntegrator/CCInegrator/./temp/TankWar_P3/src/Tank$$einfrieren.java
*/
//#if TankWar_P1 | TankWar_P2 | TankWar_P3
import java.util.Random;
import java.util.Vector;
//#if TankWar_P1
abstract class Tank$$einfrieren extends Tank$$Tools {
//#elif TankWar_P2 | TankWar_P3
import java.io.IOException;
abstract class Tank$$einfrieren extends Tank$$Beschleunigung {
//#endif
protected long einfrierenTimer;
protected boolean einfrieren = false;
public Tank$$einfrieren(TankManager tankManager, int x_Koordinate, int y_Koordinate, int tankType) {
super(tankManager, x_Koordinate, y_Koordinate, tankType);
}
protected void toolKontroller() {
super.toolKontroller();
if (tankManager.status == GameManager.PAUSE || tankManager.status == GameManager.EXIT) {
if (einfrieren) {
einfrierenTimer += elapsedTime;
}
}
if (einfrieren && System.currentTimeMillis() - einfrierenTimer > 8000) {
for (int i = 0; i < tankManager.tankMenge.size(); i++) {
if (tankManager.tankMenge.elementAt(i) != ((Tank) this)) {
((Tank) tankManager.tankMenge.elementAt(i)).aktive = true;
}
}
einfrieren = false;
}
}
protected void toolBehandeln(int toolType) {
super.toolBehandeln(toolType);
switch(toolType) {
case 371:
((Tank) this).einfrierenTimer = System.currentTimeMillis();
((Tank) this).einfrieren = true;
for (int i = 0; i < tankManager.tankMenge.size(); i++) {
if (tankManager.tankMenge.elementAt(i) != tankManager.tank1 && tankManager.tankMenge.elementAt(i) != tankManager.tank2) {
((Tank) tankManager.tankMenge.elementAt(i)).aktive = false;
}
}
break;
}
}
}
//#endif